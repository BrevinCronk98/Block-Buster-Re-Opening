@{
    Layout ="_Layout";
}

@using BlockBuster.Models;
@model IEnumerable<BlockBuster.Models.Movie>;

<h1>Movies for @User.Identity.Name</h1>

@if(Model.Any())
{
    <ul>
    @foreach(Movie movie in Model)
    {
        <li>@Html.ActionLink($"{movie.Name}", "Details", new { id = movie.MovieId})</li>
    }
    </ul>
}
else
{
      <h3> No Movies Have Been Added Yet!</h3>
}


<p>@Html.ActionLink("Add a new Movie", "Create")</p>
<p>@Html.ActionLink("Home Page", "Index", "Home")